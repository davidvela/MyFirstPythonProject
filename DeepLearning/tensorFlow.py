import numpy as np 

# forward propagation - multiply input for weights 
input_data = np.array([2,3])
weights = { 'node_0': np.array([1, 1]),
            'node_1': np.array([1-, 1]),
            'output': np.array([2, -1])}
